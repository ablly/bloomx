# BloomX 前端设计系统

## 产品定位
BloomX 不是普通 API 文档站，而是“二手大模型 API 能力交易市场”。商家上传自己的模型名称、Base URL、API Key、价格和售后规则；平台先真实测试 API，测试通过后才允许上架；用户购买或订阅积分套餐后，按商家、模型、价格选择并调用；成功调用计入商家收入，失败调用退款并进入售后记录。

## 视觉方向
- 关键词：可信交易、技术流动、清晰结算、低噪声控制台。
- 背景：深色非纯黑底色，叠加低饱和视频和 PlayCanvas WebGL 交易网络层，表现“买家、平台路由、商家供给”的流动关系。
- 主色：深墨绿 `#07100f`，暖橙 `#d76f37`，结算绿 `#2f6f5e`，少量冷蓝 `#355c8a`。
- 禁止：大面积紫蓝霓虹、纯黑背景、看不清输入内容的浅色输入框、无意义装饰卡片。

## 组件规则
- 输入框必须使用深色或透明玻璃背景，文字为高对比白色，placeholder 保持低对比但可读。
- 按钮分三类：主操作白底深字、次操作深色描边、危险操作红色文字。所有按钮必须有 hover、active、disabled 状态。
- 图标优先使用项目已安装的 lucide-react；图标只表达动作，不堆砌装饰。
- 卡片用于数据实体：商家、API 商品、订阅、调用记录、结算记录。页面分区不要套娃式卡片。

## 交互规则
- 商家上架 API 前必须先测试连通性；失败不得上架。
- 用户调用 API 前必须有有效订阅、足够积分和平台 API Key。
- 商家 API 跑不通时必须给用户明确提示，并保留记录供售后核查。
- 中文模式必须所有业务文案为中文；英文模式必须所有业务文案为英文。

## WebGL 规则
- WebGL 只作为背景和空间提示，不遮挡表单、按钮、数据表。
- 动效基于 transform/opacity/PlayCanvas 场景更新，避免布局抖动。
- 尊重 `prefers-reduced-motion`，弱化动画幅度。
