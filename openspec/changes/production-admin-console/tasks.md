# 任务清单

## 实现

- [x] 新增后台数据读取服务，统一读取运营集合并归一化记录。
- [x] 新增运营总览指标、待办队列、生产风险和模块覆盖视图。
- [x] 将 `/admin` 升级为完整侧边栏后台控制台。
- [x] 覆盖用户、商家、商品、订单、支付、账本、退款、工作流、Webhook、结算、审计、配置模块。
- [x] 增加搜索、状态筛选、空状态、读取失败状态和敏感动作禁用态。
- [x] 保留未登录预览，同时阻止非管理员登录用户访问敏感后台。

## 验证

- [x] `npm run build`
- [x] `npm run spec:validate:strict`
- [x] 本地路由访问 `/admin` 和主要后台子路由。
- [x] 自审后台不再使用假支付/账本/Webhook 数据。
- [x] 自审没有把真实密钥写入前端。

