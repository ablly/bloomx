# BloomX 交付协作协议

本项目采用 OpenSpec + Superpowers 的规格驱动交付方式。除非用户明确要求跳过，所有非平凡功能、架构、自动化、前端或 UI 改动都必须先进入规格闭环，再写代码。

## 文档语言

- 以后面向用户、项目协作、交付说明、设计说明、规格说明、计划总结的文档默认都使用中文。
- OpenSpec、代码、命令、字段名、接口名、文件名、枚举值可以保留英文。
- 如果第三方工具强制要求英文标题或格式，保留必要英文结构，但正文说明使用中文。

## 工作方式

1. 先判断任务类型：
   - 目标或方案不清楚时，先探索并提出选择。
   - 明确要做的功能，先创建或更新 OpenSpec change。
   - 只有小修、小文案、单点 bug 可以直接改，但仍需说明验证结果。
2. OpenSpec change 必须包含：
   - `proposal.md`：为什么做、做什么、不做什么。
   - `specs/`：用 ADDED / MODIFIED / REMOVED 写行为要求和场景。
   - `design.md`：技术方案、数据流、UI 决策、风险。
   - `tasks.md`：可勾选的实现和验证清单。
3. 实现时按 `tasks.md` 前进；如果发现规格不对，先更新规格或设计，再继续写代码。
4. 完成前必须做交付检查：
   - OpenSpec change 可以通过 `openspec validate` 校验。
   - 对应任务全部勾选，或明确记录未完成原因。
   - 运行项目相关构建、测试或最小验证。
   - UI 改动必须通过 taste + Open Design 审查。
   - 总结实际改动、验证结果、剩余风险。

## n8n 工作流规则

- 后续所有新的流程自动化、Webhook 编排、业务事件投递、运营通知和跨系统流程，默认都使用 n8n。
- Make.com 相关资料只作为历史记录或迁移参考；除非用户明确要求，不再作为新流程的默认实现。
- 涉及自动化的 OpenSpec `design.md` 必须说明对应 n8n 工作流、触发事件、输入输出、失败记录和验收方式。
- 如果某个流程不能使用 n8n，必须在 `design.md` 中说明原因、替代方案和回滚方式。
- 涉及 n8n 的交付必须提供可访问页面或入口，方便用户查看工作流名称、编辑页、Webhook URL 和验证方式。

## 完成后的运行与自审

- 每次完成一段工作后，必须运行项目相关命令；前端改动至少运行 `npm run build`，规格改动至少运行 `npm run spec:validate:strict`。
- 完成后必须自审：检查本次范围内的 bug、乱码、构建警告、规格遗漏、UI 状态、n8n 流程入口和剩余风险。
- 如果自审发现问题，继续修复并重新运行验证，直到当前范围内没有阻塞问题。
- 最终回复必须说明：运行了什么、发现了什么、修复了什么、还剩什么风险。

## GitHub 推送规则

- 当前范围自审无阻塞问题后，默认提交并推送到 GitHub。
- 提交前必须只 stage 本次工作相关文件，不能把历史未整理改动混入同一个提交。
- 如果工作区存在无关改动，保持原样，不回滚、不纳入提交。
- 推送前必须确认当前分支、提交内容和验证结果。

## Superpowers 使用规则

- 规划、执行、调试、收尾时优先采用 Superpowers 的纪律：先澄清意图、再拆解任务、逐项验证、最后收尾。
- 执行已有计划时，严格跟随计划；遇到阻塞先说明阻塞，而不是跳过验证。
- 代码 review 以风险、bug、回归、缺失测试为先，不做泛泛审美评价。

## UI 与前端设计规则

所有前端、交互和视觉设计默认使用两层设计约束：

1. `design-taste-frontend` 作为执行审美：
   - 避免默认 AI 紫蓝渐变、过度卡片、空洞指标、emoji 装饰。
   - 仪表盘和交易工作台优先使用清晰层级、稳定密度、真实状态。
   - 动效只用 transform / opacity，尊重 `prefers-reduced-motion`。
   - 表单必须有完整 loading、empty、error、disabled、active 状态。
2. Open Design 作为 artifact-first 设计流程：
   - 先提炼品牌和产品上下文，再产出界面。
   - UI 设计要有可检查的设计系统、组件约束和自评清单。
   - 如果没有真实素材，使用诚实占位，不编造数据。
   - 设计交付必须可预览、可修改、可继续迭代。

## BloomX 产品边界

BloomX 是大模型 API 能力交易市场：商家提交模型 API，平台先测试再上架；用户购买或订阅积分后调用；成功调用计入商家收入，失败调用退款并留下售后记录。

后续所有功能必须服务这个闭环，避免添加与交易、订阅、调用、审核、结算、售后无关的填充内容。
